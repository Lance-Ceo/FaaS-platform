apiVersion: 1

providers:
  - name: FaaS Platform
    orgId: 1
    folder: FaaS Platform
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
